# Palomar selected-root blocker map

## Current authority — 10 September 2026

Use the P0--P7 source-freeze board at the top of `PALOMAR_RELEASE_CONE.md`
and its linked audit-branch continuation for later receipts. The historical
BHP/Guth--Maynard-only plan is superseded by D31. R1--R8 closed both D31
public proofs locally; the two sharp proofs remain open on their preserved
branch. P1 repaired the stricter Challenge/Core structural identity blocker.
P2 preserved the sharp development and removed its admitted Solution and
configuration from the D31 tree. P3 reconciled the public mathematical account;
P4 passed the final full build and local audit. **P0--P4 VERIFIED (5/8)**;
public immutable source, protected Linux replay/render and handoff remain
P5--P7 at this source freeze.

## D31: project-local formalization first

D31 consumes global PNT, not a short-interval theorem. BHP, Guth--Maynard,
reciprocal tiles, and the local Lemma 9.2 core-capacity budget are not blockers
for this route.

| Item | Evidence / remaining requirement |
|---|---|
| Public statement alignment | R1 CLOSED: synchronized D31 statements, four selections and metadata; 28 types and 24 definition values match. No public proof promotion. |
| Full-star objects and S1/S2 | R2 CLOSED: literal full-star principal matrix, unit vector and positive root, S1, regular correction/logarithmic size, S2 and exact public-rank bridge freshly audited. R4 subsequently supplies the collective synthesis. |
| Finite ordered comparison | R3 CLOSED: literal Q/TQ-QD comparison, same-index Schur, HW/Weyl, squared-energy sum and weak sorting contraction; standard axioms and finite controls pass. |
| Actual global frame estimates | R4 CLOSED: actual complete-prefix symmetric whitening is an isometry in the one-exit space; both HS and corrected operator residual bounds hold uniformly on sub-square-root power prefixes. Exact linear-isometry bridge to R3 is compiled. |
| Energy-weighted complement | R5 CLOSED: actual full-prefix forest top, whitened A and one-exit G complement gaps at mu_B/16, and both residual absorptions hold uniformly on terminal B with K=12288 B. Final 4238-job build and ten standard-axiom roots are recorded in .lake/d31-r5/. The 10 September continuation matched the saved evidence to the owner/audit hashes; it did not replay that build. |
| Ordered mean-square and tail | R6 CLOSED: actual R2--R5 producers discharge every R3 premise; complete-prefix VALUE sorting preserves the global arithmetic rank, including ties, and S2 transfers at that same rank. Literal defect mean square is bounded by C X log^5 X + C B^3/log^2 X; the tail above C0 B/log X + C1 t mu_B is bounded by C2 B log^5 X/t^2 for every t>0. Fresh full build 4239, fifteen new standard-axiom roots and eight exact controls pass. |
| Public density and corollary assembly | R7 CLOSED: both revised almost-all Solution roots have standard-axiom proofs; terminal counts and full density use the same error parameters and retain the initial segment. Internal sharp density and every-centre operator corollaries also compile. Targeted/full builds 4237/4239 and fifteen controls pass. |
| Verification | R8 CLOSED locally: selected/full builds 4238/4239 pass; eight transitive D31 roots have only standard axioms; 34 shared types and 26 definition values compare in Lean. Declaration/source-reference inventory, provenance, metadata and source documentation checked. |

Two scale mismatches are explicit:
- S1's `O(a/log X)` molecule defect is enough on a complete prefix because
  sorting contracts its uniform `O(B/log X)` value error. It is not the
  paper's stronger intermediate `o(1)` claim.
- `TerminalSchur`'s compiled local coherent budget and old summed residual
  `O(B^2/log X)` do not imply the required global `O(B log(X)^C)` HS
  budget or the corrected operator bound. R4 now supplies these through the
  actual projected full-star frame and its symmetric whitening instead.

## Sharp every-centre route: separate, later

`palomar_terminalBand_prescribedRanks` retains its `21/61 < theta < 1/2`
range and literal `O(a/log X)` error.

| Item | Status |
|---|---|
| BHP 21/40 input | Separate external theorem/dependency project; not discharged by PNT or a conditional adapter. |
| Full-star local coherent/interface alignment | Existing compiled `TerminalSchur` work is preserved; recheck the repaired native matrix, projection and Gram errors before using it as the manuscript consumer. |
| Buffered capacity and rank assembly | Declaration-level reuse after the actual spectral and BHP producers exist. |
| Prime-counting corollary | Derive from the sharp root; no independent research task. |

The 8 September search found no matching unconditional BHP theorem in the
then-checked Mathlib, PNTA, Tau Ceti, CSLib or other reached public sources.
That is dated search evidence, not a refreshed assertion that no proof exists.
The separately authorized short-interval project is not cancelled or edited
by this replan. Do not develop Harman's sieve inside this release repository.

## Reuse / trust boundaries

The prior S1, S2 and local coherent producers have recorded successful builds
and standard axioms in `PORT_MANIFEST.md`. Preserve that evidence for their
literal propositions. It does not certify alignment of all repaired native
objects, a new global frame estimate, or a revised public theorem.

Reuse the existing owners and admit only the preapproved D31 owners when
their R-row starts. R3 admitted `AlmostAllSpectralBudget` and added the
finite comparison proofs; R1 changed the D31 statements and selection names,
not their proof status. `AlmostAllArithmetic` was removed from the planned
owner set before creation. The toolchain, dependencies and dependency pins
remain unchanged.

The historical source inventory at R8 was nine modules / six proof
owners / 33,559 Lean lines including lakefile. The D31 checklist is
**8/8 validated (100%)**, MG4 **2/2 COMPLETE**, public proofs **2/4**.
Only the sharp pair retains sorryAx. The eight audited D31 roots have
71,747 constants in their transitive union and no extra axiom or conditional
short-interval input. The complete candidate has zero unreachable modules;
retained shared/sharp declarations outside D31 are explicitly inventoried.

Final readiness is tracked by **P0--P7**, independently of the completed
R1--R8 proof checklist:

- P1--P4: the final full build passes 4239 jobs. Both public types, all 16
  selected definition values and 16,491 reachable shared constants pass the
  strict local comparison. Eight public/internal proof closures have 71,752
  union constants and only `propext`, `Classical.choice`, `Quot.sound`.
- The D31 tree has zero Solution admissions, nine local modules, six proof
  owners and zero unreachable modules. The unchanged sharp obligations and
  configuration are preserved on `sharp-development` at the revision in
  `SOURCE_PROVENANCE.md`; they are outside this submission tree.
- All 15 exact controls pass. Current schema and Palomar metadata-contract
  checks pass. The semantic-lint rerun reproduces exactly the 93 inherited
  owner findings; the single Unicode-style finding is also unchanged. No
  clean-lint claim is made.
- P5 must establish the reviewed, clean, pushed and anonymously available
  source SHA. Before this gate, the authorized SSH repository remains private.
- P6 requires the exact current protected Linux preflight/full replay and
  rendered Challenge. An existing public verifier fork can supply the Linux
  rehearsal; no completed protected result is claimed at source freeze.
- P7 reconciles those receipts with the public account and exact manual intake
  packet. Novelty remains unestablished, no separate human line-by-line review
  is claimed, and no Palomar state change is performed by this goal.

The current local evidence is `.lake/palomar-readiness-2026-09-10/`; earlier
`.lake/d31-r8/` evidence remains historical. The manuscript, toolchain,
manifest, lakefile and D31 selection pins remain unchanged. Exact source and
post-freeze receipts continue on the audit branch linked from the task board.
