# Palomar selected-root blocker map

## Current authority — 9 September 2026

Use the D31 rebase at the top of `PALOMAR_RELEASE_CONE.md`.
The old rule that only BHP or Guth--Maynard work could proceed is superseded.
The paper proof is internal; the revised D31 Challenge and Solution are not
implemented. Four Solution holes remain. No fresh build is claimed here.

## D31: project-local formalization first

D31 consumes global PNT, not a short-interval theorem. BHP, Guth--Maynard,
reciprocal tiles, and the local Lemma 9.2 core-capacity budget are not blockers
for this route.

| Item | Evidence / remaining requirement |
|---|---|
| Public statement alignment | R1: old Challenge uses `theta > 1/6`, includes `X/a^2`, and compares with Weyl. Amend to Theorem 1.3 and Corollary 1.4 together; no isolated metadata promotion. |
| Full-star objects and S1/S2 | Existing `exactPrincipalMoleculeSupport` contains the full boundary and invariant first-exit support, including singleton exits. R2 must revalidate the matrix/vector/normalization and scalar consumers against the repaired paper. |
| Finite ordered comparison | R3: reuse Hermitian HW, ordered Weyl, Schur and sorting APIs. The exact same-rank low-complement composition is new local assembly. |
| Actual global frame estimates | R4: prove isometry, global HS and corrected operator bounds on the same full-star frame. A finite conditional normalization theorem does not discharge these arithmetic estimates. |
| Energy-weighted complement | R5: instantiate the full-prefix complement gap and smallness, with global PNT. Do not use target-width sorting or the BHP buffered argument as a prerequisite. |
| Mean-square and density assembly | R6--R7: discharge every R3 premise using R2/R4/R5, derive the tail and dyadic counts, then close the revised public roots. |
| Verification | R8: fresh selected-root build, standard-axiom cone, statement comparison, provenance and minimality checks. |

Two scale mismatches are explicit:
- S1's `O(a/log X)` molecule defect is enough on a complete prefix because
  sorting contracts its uniform `O(B/log X)` value error. It is not the
  paper's stronger intermediate `o(1)` claim.
- `TerminalSchur`'s compiled local coherent budget and old summed residual
  `O(B^2/log X)` do not imply the required global `O(B log(X)^C)` HS
  budget or the corrected operator bound. R4 remains a real obligation.

## Sharp every-centre route: separate, later

`palomar_terminalBand_prescribedRanks` retains its `21/61 < theta < 1/2`
range and literal `O(a/log X)` error.

| Item | Status |
|---|---|
| BHP 21/40 input | Separate external theorem/dependency project; not discharged by PNT or a conditional adapter. |
| Full-star local coherent/interface alignment | Existing compiled `TerminalSchur` work is preserved; recheck the repaired native matrix, projection and Gram errors before using it as the manuscript consumer. |
| Buffered capacity and rank assembly | Declaration-level reuse after the actual spectral and BHP producers exist. |
| Prime-counting corollary | Derive from the sharp root; no independent research task. |

The 8 September search found no matching unconditional BHP theorem in the
then-checked Mathlib, PNTA, Tau Ceti, CSLib or other reached public sources.
That is dated search evidence, not a refreshed assertion that no proof exists.
The separately authorized short-interval project is not cancelled or edited
by this replan. Do not develop Harman's sieve inside this release repository.

## Reuse / trust boundaries

The prior S1, S2 and local coherent producers have recorded successful builds
and standard axioms in `PORT_MANIFEST.md`. Preserve that evidence for their
literal propositions. It does not certify alignment of all repaired native
objects, a new global frame estimate, or a revised public theorem.

Use the existing four owners and admit only the two already planned D31
owners when their R-row starts. `AlmostAllArithmetic` is removed from the
planned owner set before creation. No proof file, toolchain, dependency,
Comparator selection, or metadata theorem claim changed in this planning pass.

Current source inventory remains seven modules / 20,243 Lean lines including
lakefile. The new R-checklist is 0/8 validated; historical 15/28 is retained
only against its original contract. No commit or push was authorized.
